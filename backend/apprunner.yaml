# backend/apprunner.yaml   ← place in the /backend folder
version: 1.0
runtime: python3           # or python311

build:
  commands:
    build:                 # <-- REQUIRED key name
      - pip install --no-cache-dir -r requirements.txt

run:
  command: uvicorn main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
  env:
    - name: OPENAI_API_KEY
      value: arn:aws:secretsmanager:us-west-2:123456789012:secret:kfranz-hackathon-openai-api-key
